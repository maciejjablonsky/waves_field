#include "pc_input.hpp"

wf::systems::pc_input::pc_input(gsl::not_null<GLFWwindow*> glfw_window)
{
    //glfwSetCursorPosCallback(glfw_window, mouse_callback_fn);
    // glfwSetScrollCallback(glfw_window_, scroll_callback_fn);
}
